@(Html.Kendo().DropDownList()
    .Name("CategoryDropDownList")
    .DataTextField("CName")
    .DataValueField("CId")
    .DataSource(source =>
    {
        source.Read(read =>
        {
            read.Action("GetCategory", "Home"); // Set the Action and Controller names.
        })
        .ServerFiltering(true); // If true, the DataSource will not filter the data on the client.
        })
        )
